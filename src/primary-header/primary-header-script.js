// const div = document.getElementById('primaryHeaderContents');

// // Fetch the external HTML file
// fetch('primary-header/primary-header.html')
//   .then(response => response.text())
//   .then(data => {
//     // Insert the HTML content into the specified div
//     div.innerHTML = data;

//     // Fetch the external JavaScript file
//     const script = document.createElement('script');
//     script.src = 'primary-header/primary-header-motion-script.js';

//     // Append the script to the document body
//     document.body.appendChild(script);
//   });
